# Force pnpm usage - this project requires pnpm
# Do not use npm or yarn for this project
packageManager=pnpm

# pnpm specific configurations
node-linker=hoisted
auto-install-peers=true

# Strict mode settings
save-exact=true
progress=false